---
const stages = [
  {
    symbol: '期待',
    title: '先说清“大象”',
    detail: '人究竟希望模型解决什么，哪些差异必须保留。'
  },
  {
    symbol: 'Y',
    title: '划定问题空间',
    detail: '定义输出、等价关系、结构、分解方式与损失。'
  },
  {
    symbol: 'D',
    title: '寻找有效监督',
    detail: '用数据覆盖、标注、关系或自监督提供任务相关约束。'
  },
  {
    symbol: 'fθ',
    title: '吸收并复用规律',
    detail: '以容量、架构和优化让约束进入共享参数。'
  },
  {
    symbol: '效用',
    title: '确认“门关上”',
    detail: '检查代理目标的改善是否兑现为真实效果。'
  }
]
---

<figure
  class='not-prose my-8 rounded-xl border border-border/60 bg-card p-3 sm:p-5'
  aria-labelledby='scaling-loop-title scaling-loop-caption'
>
  <div class='mb-4 flex flex-col gap-1'>
    <p id='scaling-loop-title' class='m-0 text-base font-semibold text-foreground'>
      从人的期待到可 Scaling 的学习任务
    </p>
    <p class='m-0 text-sm leading-6 text-muted-foreground'>
      问题定义给出学习方向，数据和模型规模沿着这个方向发挥作用。
    </p>
  </div>

  <ol class='m-0 grid list-none gap-0 border-y border-border/60 p-0 lg:grid-cols-5' role='list'>
    {
      stages.map((stage, index) => (
        <li
          class:list={[
            'py-4 lg:px-3',
            index > 0 && 'border-t border-border/60 lg:border-l lg:border-t-0'
          ]}
        >
          <div class='mb-2 flex items-center gap-2 lg:block'>
            <span class='inline-flex h-8 min-w-8 items-center justify-center rounded bg-primary/15 px-2 text-sm font-semibold text-foreground'>
              {stage.symbol}
            </span>
            <span class='font-semibold text-foreground lg:mt-2 lg:block'>{stage.title}</span>
          </div>
          <p class='m-0 text-sm leading-6 text-muted-foreground'>{stage.detail}</p>
        </li>
      ))
    }
  </ol>

  <div class='mt-4 grid gap-2 text-sm leading-6 sm:grid-cols-2'>
    <p class='m-0 border-l-2 border-primary pl-3 text-foreground'>
      <span class='font-semibold'>数据 Scaling：</span>增加样本数、覆盖与有效约束。
    </p>
    <p class='m-0 border-l-2 border-primary pl-3 text-foreground'>
      <span class='font-semibold'>模型 Scaling：</span>扩大 fθ 的容量与可优化性。
    </p>
  </div>

  <figcaption id='scaling-loop-caption' class='mt-4 text-sm leading-6 text-muted-foreground'>
    图 1：能装进去，意味着问题空间对期待足够充分；能关上门，意味着训练结果通过了真实效用检验。
  </figcaption>
</figure>
